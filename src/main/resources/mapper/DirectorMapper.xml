<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.mapper.DirectorMapper">
    <select id="searchByName" resultType="com.movie.entity.Director">
        SELECT * FROM director WHERE name LIKE CONCAT('%', #{name}, '%') OR english_name LIKE CONCAT('%', #{name}, '%')
    </select>
</mapper>